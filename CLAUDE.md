# rus-pharma-web-app

## Описание

Электронная рабочая тетрадь по курсу **«Фармакология и обработка ран в боевых условиях в отдалённой местности»**.

Целевая аудитория — курсанты/студенты, изучающие тактическую медицину. Приложение включает систему авторизации по группам, компетенционную модель прогресса, флэш-карточки с RPG-элементами, тестирование, матрицу прогресса и офлайн-режим.

**Продакшн:** https://ios9000.github.io/rus-pharma-web-app  
**Репозиторий:** https://github.com/ios9000/rus-pharma-web-app

---

## Стек

### Текущий (студенческий фронтенд)
- **Frontend:** HTML / CSS / JavaScript (vanilla, без фреймворков, без бандлеров)
- **Бэкенд / API:** Google Sheets API (облачная синхронизация)
- **Хостинг:** GitHub Pages
- **Офлайн:** Service Worker

### Кабинет инструктора
- **Supabase** (PostgreSQL + Auth + Storage)
  - URL: https://otoxfxwwdbeblwpizlbi.supabase.co
  - JS-клиент через CDN (@supabase/supabase-js)
  - Конфиг: instructor/js/supabase-config.js
- **Синхронизация:** Google Apps Script (SupabaseSync.gs) — Supabase → Google Sheets

### Целевой (миграция в процессе)
- **БД:** Supabase (PostgreSQL) — вопросы, препараты, сценарии, компетенции
- **Auth:** Supabase Auth — роли instructor / student, JWT, Row Level Security
- **Storage:** Supabase Storage — иллюстрации к вопросам и препаратам
- **Серверная логика:** Supabase Edge Functions — вызовы Claude API, синхронизация
- **LLM:** Claude API (Anthropic) — парсинг лекций → генерация контента

### Стратегия миграции (параллельный запуск)
1. **Сейчас:** кабинет инструктора пишет в Supabase
2. **Промежуточно:** Google Apps Script синхронизирует Supabase → Google Sheets (студенты работают как прежде)
3. **Потом:** студенческий фронтенд переключается на Supabase, Google Sheets убирается

---

## Структура проекта

```
rus-pharma-web-app/
├── .claude/
│   ├── CLAUDE.md              ← этот файл
│   └── rules/                 ← правила для Claude Code
│
├── docs/
│   ├── README.md
│   ├── CHANGELOG.md
│   ├── roadmap.md             ← план итераций
│   ├── project-architecture.md ← архитектура системы
│   └── iterations/
│       ├── 003_multiple-choice/
│       ├── 004_instructor-cabinet/
│       ├── 004.5_user-management/
│       ├── 005_llm-automation/
│       └── 006_structured-testing/
│
├── css/                       ← стили, разделены по модулям
├── js/                        ← модули приложения (см. ниже)
├── instructor/                    ← кабинет инструктора (SPA)
│   ├── index.html
│   ├── css/instructor.css
│   └── js/
│       ├── supabase-config.js     ← URL + anon key
│       ├── supabase-client.js     ← инициализация клиента
│       ├── auth.js                ← логин/логаут Supabase Auth
│       ├── app.js                 ← роутинг секций
│       ├── questions.js           ← CRUD вопросов
│       ├── drugs.js               ← CRUD препаратов
│       ├── image-upload.js        ← загрузка в Storage
│       ├── instructors.js         ← реестр инструкторов
│       ├── cadets.js              ← управление курсантами
│       ├── groups.js              ← управление группами
│       ├── text-extractor.js      ← извлечение текста из PDF/DOCX/TXT
│       ├── llm-generator.js       ← UI генерации контента через Claude API
│       └── review-cards.js        ← ревью и одобрение сгенерированного контента
├── supabase/
│   └── functions/
│       └── generate-content/
│           └── index.ts           ← Edge Function: Claude API, парсинг, сохранение
├── index.html                 ← точка входа (студенческий интерфейс)
├── service-worker.js          ← кэширование для офлайн-режима
└── ...
```

---

## Ключевые модули (js/)

| Файл | Назначение |
|---|---|
| `competencies-config.js` | Конфигурация 26 компетенций в 6 блоках; экспортирует `COMPETENCIES_CONFIG` |
| `competency-progress.js` | Отслеживание прогресса по компетенциям |
| `progress-matrix-ui.js` | UI матрицы прогресса (визуализация) |
| `test-selector.js` | Выбор тестов / вопросов для тренировки |
| `auth-*.js` | Авторизация: группы, PIN-коды, офлайн-логин, облачная синхронизация |

**Контент:** 81 вопрос, 6 препаратов, сценарии (источник — rus_pharma_field_advanced.xlsx → Google Sheets).

---

## Supabase (кабинет инструктора)

**URL:** `otoxfxwwdbeblwpizlbi.supabase.co`

**Таблицы:**
- `competencies` — 26 компетенций в 6 блоках (id, name, block, description)
- `questions` — вопросы (id, question_text, options JSONB[], correct_answer, competency_id, competency_block, category, module, difficulty, image_url, item_type)
- `drugs` — препараты + flash-карточки (id, name_ru, name_lat, drug_group, dosage, form, indications, contraindications, side_effects, field_notes, image_url, item_type: 'drug'|'device'|'instrument'|'equipment')
- `instructors` — реестр инструкторов (id UUID, email, full_name, created_by, is_active)
- `cadets` — курсанты (id TEXT, group_code, full_name, pin_code, is_active)
- `groups` — учебные группы (code TEXT PK, name, instructor, max_cadets, is_active)
- `sync_log` — лог синхронизации (автоматический через триггеры)
- `generated_content` — сгенерированный LLM-контент (id, generation_id, type, content JSONB, status: draft|approved|rejected, competency_id, instructor_id, approved_target_id). RLS: инструктор видит только свои
- `generation_log` — лог запросов к Claude API (generation_id, source_text_length, types_requested, tokens_input, tokens_output, model, duration_ms, status, error_message). RLS: инструктор видит только свои

**Auth:** Email provider, роль instructor в user_metadata

**Storage:** Бакет content-images (public), путь: {user_id}/questions/ и {user_id}/drugs/

**Edge Function:** `generate-content` (Deno, TypeScript, 454 строки)
- Claude Sonnet 4 (`claude-sonnet-4-20250514`), max_tokens 4096
- 4 типа контента: question, drug, flashcard, scenario
- JWT verification отключена в конфиге, проверка через `auth.getUser()` в коде
- AbortController timeout 90 сек, обработка 429 rate limit
- Сохраняет в `generated_content`, логирует в `generation_log`

**Разделение данных:**
- Google Sheets — оригинальные данные (81 вопрос, 6 препаратов)
- Supabase — новые данные из кабинета инструктора + LLM-генерация
- Маркер `[SB]` в ID отличает синхронизированные строки от оригинальных

**Синхронизация:** SupabaseSync.gs (Google Apps Script) → Supabase REST API → Google Sheets, маркер [SB], setNumberFormat('@') для колонки E. Оригинальные строки без маркера никогда не удаляются

---

## Правила для Claude Code

### Код
- Код (переменные, функции, классы) — на **английском**
- Комментарии допустимы на **русском**
- Перед пушем убедиться, что нет `console.log` в продакшн-коде
- Не менять архитектуру без согласования (архитектурные решения обсуждаем в Claude.ai)

### Git-коммиты
- Язык: **английский**
- Формат: conventional commits
  - `feat: add triage module UI`
  - `fix: correct auth token handling`
  - `docs: update iteration 003 session log`
  - `refactor: extract validation into separate module`
- Один коммит = одна логическая единица

### Чего НЕ делать
- Не объединять все изменения в один коммит "update"
- Не менять файлы в `docs/iterations/` без явного указания
- Не вводить новые зависимости / библиотеки без согласования
- Не удалять закомментированный код без подтверждения
- Не трогать студенческий фронтенд при работе над кабинетом инструктора (если не указано иное)

---

## Известные проблемы

- [ ] `sync-module.js` — GET-запрос прогресса слишком длинный → 400 Bad Request (URL > лимита)
- [ ] Поля «Категория», «Модуль» в форме вопросов (в БД есть, в UI — нет)
- [ ] Поле «Побочные эффекты» в форме препаратов (в БД есть, в UI — нет)
- [ ] Создание Auth-пользователей инструкторов из кабинета (сейчас — через Supabase Dashboard)

---

## Текущая итерация

Завершена: **005 — LLM Automation** (см. docs/iterations/005_llm-automation/)
Следующая: **006 — Structured Testing** (см. docs/iterations/006_structured-testing/)

---

## Roadmap

| # | Итерация | Статус |
|---|---|---|
| 000–002 | Стабилизация (подключение модулей, SW, контент) | ✅ Завершено |
| 003 | Multiple Choice (checkbox) | ✅ Завершено |
| 004 | Кабинет инструктора MVP (формы, Supabase, auth) | ✅ Завершено |
| 004.5 | Управление пользователями (группы, курсанты, инструкторы) | ✅ Завершено |
| 005 | LLM Automation (парсинг лекций → контент через Claude API) | ✅ Завершено |
| 006 | Structured Testing (структурированное тестирование) | 🔄 В работе |
| 007 | Migration (полная миграция с Google Sheets на Supabase) | 📋 Планируется |
| 008 | Offline Mode (Service Worker, offline-first) | 📋 Планируется |

---

## История стабилизации (Sprint 0–2, завершено)

- **Sprint 0:** Подключены 4 отключённых JS-модуля к index.html, исправлены критические баги
- **Sprint 1:** Централизация API URL, устранение конфликтов DOMContentLoaded, модалка изображений, иконки
- **Sprint 2:** Service Worker для офлайн-режима, fallback-данные из реального контента (81 вопрос, 6 препаратов)
